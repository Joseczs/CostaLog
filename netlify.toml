[build]
  publish = "public"
  command = ""

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 404

# Nota: este redirect 404 es solo fallback. La app es multi-página
# (no SPA con router), así que cada .html se sirve directo por su ruta.
